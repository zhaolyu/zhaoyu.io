pnpm-lock.yaml
build/
.svelte-kit/
/design-system/

# Generated by `pnpm og` from the prerendered /og/cards.json route, which emits
# JSON.stringify(..., 2). Prettier would collapse its short string arrays onto
# one line, so every `pnpm format` and the next `pnpm og` would fight over it.
static/og/manifest.json
