---
globs: *.js,*.ts,*.svelte
---

# Coding Conventions

## ESLint Configuration

- Base: Project-specific config (or SvelteKit recommended config)
- Parser: TypeScript for `.ts` files, Svelte for `.svelte` files
- Key rules: no-console (error), semicolons required

## Prettier

- Print width: 100 characters
- Single quotes for JavaScript/TypeScript
- Trailing commas: all
- Configuration: [.prettierrc](mdc:.prettierrc) or project config

## Naming Conventions

- **Components**: PascalCase (`Welcome`, `Layout`, `ProjectCard`)
- **Functions/Variables**: camelCase (`fetchData`, `userName`)
- **Constants**: UPPER_SNAKE_CASE (`API_BASE_URL`)
- **Files**: 
  - Svelte components: PascalCase (e.g., `Welcome.svelte`)
  - Utilities: camelCase (e.g., `formatDate.ts`)

## Imports

Use SvelteKit's `$lib` alias for lib imports, or relative imports:

```typescript
// Relative imports
import Component from '../components/Component';
import { formatDate } from '../lib/utils/date';

// SvelteKit $lib alias (recommended)
import { formatDate } from '$lib/utils/date';
import { theme } from '$lib/stores/theme';
```

## Component Structure

### Svelte Components

```svelte
<script lang="ts">
	// Component script (TypeScript)
	interface Props {
		title: string;
		count?: number;
	}

	let { title, count = 0 }: Props = $props();
</script>

<div class="component">
	<h1>{title}</h1>
	{#if count > 0}
		<p>Count: {count}</p>
	{/if}
</div>

<style>
	.component {
		padding: 1rem;
	}
</style>
```

### Svelte Components with Reactivity

```svelte
<script lang="ts">
	let count = $state(0);

	function increment() {
		count++;
	}
</script>

<button on:click={increment}>
	Clicked {count} times
</button>
```

## Code Style

- 2 spaces indentation (tabs converted to spaces)
- Single quotes for strings
- Always use semicolons
- Always use braces for if/for/while

## Comments and Documentation

**Minimize inline comments** - Write self-documenting code through clear naming and structure. Only add inline comments when the code's intent is genuinely unclear.

**Use JSDoc/TSDoc for function/component documentation** - Document public APIs, complex logic, and non-obvious behavior:

```typescript
/**
 * Formats a date string according to the specified format pattern.
 * @param date - The date to format
 * @param format - Format pattern (e.g., 'MM/DD/YYYY')
 * @returns Formatted date string
 */
const formatDate = (date: Date | string, format: string): string => {
	// Implementation
};
```

**Avoid:**
- Redundant comments that restate what the code does
- Commented-out code (remove it instead)
- Excessive inline explanations for straightforward code

**Prefer:**
- Clear variable and function names
- JSDoc/TSDoc for exported functions and components
- Brief comments only for complex business logic or non-obvious behavior

See [.cursor/docs/CODING_CONVENTIONS.md](mdc:.cursor/docs/CODING_CONVENTIONS.md) for complete coding conventions.
